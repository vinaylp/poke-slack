{
  "name": "slack-poke-integration",
  "version": "1.0.0",
  "description": "Scheduled Slack integration that automatically forwards new messages to Poke via webhook",
  "main": "api/cron/sync.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "slack",
    "poke",
    "webhook",
    "integration",
    "vercel",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "@slack/web-api": "^7.0.0",
    "dotenv": "^16.4.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
